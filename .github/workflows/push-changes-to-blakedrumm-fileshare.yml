# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      - name: Copy Operations Manager Powershell to Blake Drumm - File Repository
        # You may pin to the exact commit or the version.
        # uses: AbleLincoln/push-to-sftp@a6c117bb60fdc94ab8e5b139cdb0b19bea582c81
        uses: AbleLincoln/push-to-sftp@v1.2
        with:
          # SFTP server
          host: 140.82.2.162 # default is 
          # SFTP server port
          port: 22 # default is 22
          # SFTP username
          username: fileshare # default is 
          # SFTP password
          password: ikadSVsSyvSrc$9e794x18d$ # default is 
          # Source directory to upload from (will upload all files in this directory)
          sourceDir: /Powershell # default is 
          # Remote directory to upload to (WARNING: overwrites ALL files in this directory)
          targetDir: /var/www/blakedrumm_fileshare/Powershell/OperationsManager/Single Files # default is 
          
      - name: Copy AutomatedLab Powershell to Blake Drumm - File Repository
        # You may pin to the exact commit or the version.
        # uses: AbleLincoln/push-to-sftp@a6c117bb60fdc94ab8e5b139cdb0b19bea582c81
        uses: AbleLincoln/push-to-sftp@v1.2
        with:
          # SFTP server
          host: 140.82.2.162 # default is 
          # SFTP server port
          port: 22 # default is 22
          # SFTP username
          username: fileshare # default is 
          # SFTP password
          password: ikadSVsSyvSrc$9e794x18d$ # default is 
          # Source directory to upload from (will upload all files in this directory)
          sourceDir: /AutomatedLab # default is 
          # Remote directory to upload to (WARNING: overwrites ALL files in this directory)
          targetDir: /var/www/blakedrumm_fileshare/Powershell/AutomatedLab/OperationsManager # default is 
          
          
      - name: Copy Operations Manager SQL Queries to Blake Drumm - File Repository
        # You may pin to the exact commit or the version.
        # uses: AbleLincoln/push-to-sftp@a6c117bb60fdc94ab8e5b139cdb0b19bea582c81
        uses: AbleLincoln/push-to-sftp@v1.2
        with:
          # SFTP server
          host: 140.82.2.162 # default is 
          # SFTP server port
          port: 22 # default is 22
          # SFTP username
          username: fileshare # default is 
          # SFTP password
          password: ikadSVsSyvSrc$9e794x18d$ # default is 
          # Source directory to upload from (will upload all files in this directory)
          sourceDir: /SQL Queries # default is 
          # Remote directory to upload to (WARNING: overwrites ALL files in this directory)
          targetDir: /var/www/blakedrumm_fileshare/SQL-Queries/OperationsManager # default is 

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
